app = "maxeffo-bot"
primary_region = "waw"

[build]
  dockerfile = "Dockerfile"

# Persistent volume — guild_context.json and logs/ live here
[[mounts]]
  source = "bot_data"
  destination = "/data"
  initial_size = "1gb"

[deploy]
  strategy = "immediate"